# Moove Propostas

Next.js 16 App Router · Prisma · PostgreSQL · Auth.js v5.

- Admin: `/dashboard`, `/pipeline`, `/propostas`, `/clientes`
- Pública: `/p/[slug]` — sem chrome administrativo
- Auth: Credentials, papéis ADMIN / COMERCIAL / VISUALIZACAO
- Tenant: `organizationId` em toda consulta
- Marca: wordmark (degradê), mark (símbolo 3D), lockup (mono)

Nunca aceitar valores monetários só do cliente na publicação — persistir do catálogo/servidor.
